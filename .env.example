# ----- Application -----
ADMIN_SECRET=change-me-to-a-strong-random-value
LOG_LEVEL=INFO

# ----- Database (production only — override docker-compose defaults) -----
DB_USER=chuckuser
DB_PASSWORD=change-me-to-a-strong-random-value
DB_NAME=chucknorris
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}

# ----- Deployment -----
GITHUB_REPO=your-github-username/chuck-norris-api
