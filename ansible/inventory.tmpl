[app_servers]
# Replace with your EC2 public IP from terraform output
${SERVER_IP} ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/${KEY_PAIR_NAME}.pem

[app_servers:vars]
ansible_python_interpreter=/usr/bin/python3
